# Berechtigungsmatrix und Löschprotokoll – 2026-09-10 21:29

- [x] Rollenrechte aus den tatsächlichen API-Berechtigungen vollständig ableiten
- [x] Dauerhafte Audit-Tabelle für Helfer- und Kuchenlöschungen anlegen
- [x] Einzel-, Bereichs- und Jahreslöschungen transaktional protokollieren
- [x] Löschprotokoll ausschließlich für Administratoren bereitstellen
- [x] Übersichtliche Berechtigungsmatrix für Administratoren und Planungsteam erstellen
- [x] Navigation und Routing ergänzen
- [x] Migration erzeugen, prüfen und anwenden
- [x] Unit-, Live-Datenbank-, Browser-, Typ- und Buildprüfung abschließen
- [x] Geprüften Stand als WebDev-Checkpoint sichern

Zwischenstand: Die Migration `0006_secret_argent.sql` legt ausschließlich die neue Audit-Tabelle an und wurde erfolgreich angewandt. Der isolierte Live-Datenbanktest bestätigte sechs korrekte Protokollzeilen für Einzel-, Bereichs- und Jahreslöschungen sowie die dauerhafte Speicherung nach dem Jahresreset. TypeScript-Prüfung und 41 automatisierte Tests sind erfolgreich. Für die UI-Prüfung wurde eine kurzlebige Administrator-Testsitzung aktiviert.

Administrator-Browserprüfung: Die neue Navigation „Rollen & Protokoll“ ist sichtbar. Die Matrix zeigt zwölf klar abgegrenzte Funktionsbereiche mit farbigen Rechtestufen und Erläuterungen. Zwei reale Testlöschungen werden mit deutschem Zeitpunkt, Jahr 2099, Objektart, gelöschtem Namen, Vorgang, ausführender Rolle und Snapshotdetails korrekt angezeigt.

Filterprüfung bestanden: Der Jahresfilter wechselte auf 2099. Der zusätzliche Filter „Nur Helfer“ reduzierte das Audit auf exakt eine Helferzeile; der Kucheneintrag war nicht mehr im Protokoll enthalten.

Planungsteam-Browserprüfung: Die Berechtigungsmatrix bleibt vollständig sichtbar und dient als selbsterklärende Rollenübersicht. Auditfilter und Auditzeilen werden nicht geladen oder angezeigt; stattdessen erscheint ausschließlich der Hinweis, dass das Löschprotokoll Administratoren vorbehalten ist. Der direkte API-Zugriff ist zusätzlich durch einen FORBIDDEN-Regressionstest abgesichert.

Beide Rollenansichten liefen ohne Browserkonsolenfehler. Die kurzlebige Planungsteam-Testsitzung wurde vollständig beendet und aus dem Browser entfernt.

Unabhängiger Review: Die Löschpfade wurden zusätzlich gegen parallele Requests gehärtet. Einzel-, Bereichs- und Jahreslöschungen sperren ihre Snapshotzeilen exklusiv, löschen exakt die zuvor protokollierten IDs und brechen bei abweichender Löschmenge mit Transaktionsrollback ab. Der Live-Parallelitätstest bestätigte: acht reale Löschungen erzeugten exakt acht eindeutige Auditzeilen; parallele Zweitversuche und Löschversuche auf nicht vorhandene IDs erzeugten keine Protokollzeile. Helfersnapshots enthalten nun zusätzlich E-Mail, Telefon und Notiz. Die Matrix wurde an die UI angeglichen: Neue Veranstaltungsjahre können jetzt auch serverseitig nur Administratoren anlegen; Selbst-Helfer und Passwortpflichten sind präzise beschrieben.

Zweiter unabhängiger Review: keine verbleibenden Blocker. Die finale Administratoransicht zeigt die korrigierten Rechte für Jahresanlage, Ansprechpartner-Löschung und eingeschränkte Helferlöschung. Nach Bereinigung aller Testdaten ist das Audit erwartungsgemäß leer und bereit für reale Löschvorgänge.

Finale Browserkontrolle: keine Konsolenfehler. Die kurzlebige Administrator-Testsitzung und sämtliche lokalen Token-Dateien werden vor dem Checkpoint entfernt.

Abschlussprüfung: TypeScript-Prüfung erfolgreich, 41 automatisierte Tests bestanden (1 bestehender Excel-Test bewusst übersprungen), Produktions-Build erfolgreich und `git diff --check` ohne Befund. Das isolierte Testjahr 2099 ist vollständig bereinigt: 0 Auditzeilen, 0 Jahre, 0 Helfer und 0 Kucheneinträge.
