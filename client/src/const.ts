export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

/**
 * MyCrewMate verwendet im Eigenbetrieb ausschließlich die integrierte
 * Passwortanmeldung. Ein externer OAuth-Weiterleiter ist nicht erforderlich.
 */
